# Lessons Learned

Problems faced: keeping AI output reliable while handling unavailable external APIs.
Security lessons: prompt injection and XSS controls must run before model calls.
AI tuning lessons: lower temperature improves consistency for sprint reports.
Docker lessons: Redis healthchecks make rate limiting more predictable in Compose.
Future improvements: add authenticated service-to-service tokens, response caching, and continuous security scanning.
